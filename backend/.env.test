# Test Environment Configuration

# Database - Using MongoDB Atlas with test database
NODE_ENV=test
MONGODB_URI=mongodb+srv://gokul:gokul@myatlasclusteredu.tgvly.mongodb.net/taskmanager_test?retryWrites=true&w=majority&appName=myAtlasClusterEDU

# Security (use test-only values - safe for CI)
JWT_SECRET=test_jwt_secret_for_github_actions_and_local_testing
JWT_EXPIRE=1h

# Server Configuration (not used in unit tests)
PORT=5002

# CORS (for integration tests if needed)
CORS_ORIGIN=http://localhost:3000

# Logging
LOG_LEVEL=error

# Suppress warnings during testing
SUPPRESS_NO_CONFIG_WARNING=true
