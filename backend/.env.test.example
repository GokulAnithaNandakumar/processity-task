# Test Environment Configuration Example
# Copy this file to .env.test and update with your actual test values

# Database - Using MongoDB Atlas with test database
NODE_ENV=test
MONGODB_URI=mongodb+srv://username:password@cluster.mongodb.net/taskmanager_test?retryWrites=true&w=majority&appName=cluster

# Security (use test-only values, never use production secrets)
JWT_SECRET=your-test-jwt-secret-minimum-32-characters-long-for-testing-only
JWT_EXPIRES_IN=30d

# Server Configuration (not used in unit tests)
PORT=5001

# CORS (for integration tests if needed)
CORS_ORIGIN=http://localhost:3000

# Logging
LOG_LEVEL=error

# Suppress warnings during testing
SUPPRESS_NO_CONFIG_WARNING=true
