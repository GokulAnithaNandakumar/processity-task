name: Deploy Backend to Azure App Service

on:
  push:
    branches:
      - main
    paths:
      - 'backend/**'
  pull_request:
    types: [opened, synchronize, reopened, closed]
    branches:
      - main
    paths:
      - 'backend/**'

jobs:
  build_and_deploy_job:
    if: github.event_name == 'push' || (github.event_name == 'pull_request' && github.event.action != 'closed')
    runs-on: ubuntu-latest
    name: Build and Deploy Backend
    steps:
      - uses: actions/checkout@v3
        with:
          submodules: true

      - name: Set up Node.js
        uses: actions/setup-node@v3
        with:
          node-version: '20'
          cache: 'npm'
          cache-dependency-path: backend/package-lock.json

      - name: Install dependencies
        run: |
          cd backend
          npm ci

      - name: Run Database Connection Tests
        run: |
          cd backend
          npm test -- --testPathPattern="connection.test.js"

      - name: Run Utility Function Tests
        run: |
          cd backend
          npm test -- --testPathPattern="utils.test.js"

      - name: Run Authentication Tests
        run: |
          cd backend
          npm test -- --testPathPattern="auth.test.js"

      - name: Run Task API Tests
        run: |
          cd backend
          npm test -- --testPathPattern="tasks.test.js"

      - name: Install production dependencies
        run: |
          cd backend
          npm ci --omit=dev

      - name: Deploy to Azure Web App
        uses: azure/webapps-deploy@v2
        with:
          app-name: 'taskmanager-api-prod-ocwrlppzw2f4s'
          slot-name: 'Production'
          publish-profile: ${{ secrets.AZURE_WEBAPP_PUBLISH_PROFILE }}
          package: './backend'
